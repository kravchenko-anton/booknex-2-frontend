import { Title } from '@/components/ui/title/title'
import { View } from 'react-native'

const GenreCatalog = () => {
	return (
		<View>
			<Title>GenreCatalog</Title>
		</View>
	)
}

export default GenreCatalog
